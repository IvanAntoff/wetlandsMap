export interface Amenaza {
    origen: string,
    fuente: string,
    olor?: boolean,
    color?: string,
    materia?: boolean,
    materiadescripcion?: string,
    espuma?: boolean,
    algas?: boolean
    tipo: string,
    analisis?: boolean,
    tipoanalises?: string,
    resultadoanalises?: string,
    informetecnico?: boolean,
    estudioambiental?: boolean,
}